{% extends 'base.html.twig' %}



{% block body %}

    <p>Login</p>


    {{ form_start(form) }}
    <button type="submit">Se connecter</button>
    {{ form_end(form) }}

    <a href="{{ path('home') }}">Retour</a>
{% endblock %}
