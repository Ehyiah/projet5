{% extends 'base.html.twig' %}


{% block title %}
    Image
{% endblock %}


{% block body %}
    <div class="container">
        <h1>Ajout d'une nouvelle image</h1>
        <p>Formulaire de création d'une nouvelle image</p>

            {{ form_start(form) }}
            {{ form_row(form.name) }}
            <button type="submit" class="btn btn-primary">Ajout</button>
            {{ form_end(form) }}

        <div class="divider"></div>
        <a href="{{ path('home') }}">Retour</a>

    </div>


{% endblock %}
